{
  "name": "ehealth-react-boilerplate-server",
  "version": "1.0.0",
  "scripts": {
    "client": "cd .. && yarn start",
    "server": "node start.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "express": "^4.16.2",
    "request": "^2.85.0",
    "babel-register": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
